# 生态环境案卷评查评分系统 - 开发方案

## 一、项目概述

### 1.1 项目定位
为生态环境执法部门、上级评查机构、检察机关等多角色提供**案卷评查评分管理**的系统，实现评查标准统一化、评分流程规范化、统计分析可视化。

### 1.2 核心需求
- **多端支持**：Web管理后台 + 移动端（手机/平板现场评查）
- **多角色使用**：执法人员、评查人员、督察人员、检察人员
- **混合案卷类型**：行政处罚案卷、执法检查案卷、信访案件

---

## 二、技术架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                   │
├─────────────────────┬─────────────────────┬───────────────────┤
│   Web管理后台        │    移动端(手机/平板)  │    数据接口API     │
│   Vue 3 + Element+  │    Uni-app + Vue 3   │    (RESTful)       │
└─────────────────────┴─────────────────────┴───────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        服务端层                                   │
├─────────────────────┬─────────────────────┬───────────────────┤
│   API Gateway       │    业务服务层         │    认证服务        │
│   (Koa/Egg.js)      │    Node.js          │    (JWT)          │
└─────────────────────┴─────────────────────┴───────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据层                                    │
├─────────────────────┬─────────────────────┬───────────────────┤
│   MySQL 8.0         │    Redis            │    文件存储        │
│   (主数据库)        │    (缓存/会话)       │    (本地/OSS)      │
└─────────────────────┴─────────────────────┴───────────────────┘
```

### 2.2 技术栈选型

| 层次 | 技术选型 | 说明 |
|------|---------|------|
| **Web前端** | Vue 3 + Vite + Element Plus | 企业级后台管理界面 |
| **移动端** | Uni-app + Vue 3 | 一套代码，发布到iOS/Android/H5/小程序 |
| **后端框架** | Nest.js (基于Node.js) | 模块化、类型安全、企业级 |
| **数据库** | MySQL 8.0 | 主数据存储 |
| **缓存** | Redis | 会话缓存、数据缓存 |
| **文件存储** | 阿里OSS / 本地存储 | 案卷附件、报告导出 |
| **部署** | Docker + Nginx | 容器化部署 |

### 2.3 数据库设计（核心表）

```sql
-- 案卷基础表
CREATE TABLE case_file (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  case_no VARCHAR(50),           -- 案卷编号
  case_type ENUM('行政处罚','执法检查','信访'), -- 案卷类型
  case_name VARCHAR(200),        -- 案件名称
  respondent VARCHAR(100),       -- 当事人
  illegal_fact TEXT,             -- 违法事实
  case_source VARCHAR(50),       -- 案件来源
  filed_date DATE,               -- 立案日期
  closed_date DATE,               -- 结案日期
  handler_id BIGINT,             -- 承办人ID
  status ENUM('待评查','评查中','已完成') DEFAULT '待评查',
  create_time DATETIME,
  update_time DATETIME
);

-- 评查标准表
CREATE TABLE review_standard (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  standard_name VARCHAR(100),   -- 标准名称
  category VARCHAR(50),         -- 分类（程序合规/实体合法/文书规范）
  check_item VARCHAR(200),      -- 检查项
  max_score DECIMAL(5,2),       -- 满分值
  scoring_rule TEXT,            -- 评分规则
  sort_order INT,               -- 排序
  is_active BOOLEAN DEFAULT TRUE,
  create_time DATETIME
);

-- 评查记录表
CREATE TABLE review_record (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  case_id BIGINT,               -- 关联案卷
  reviewer_id BIGINT,           -- 评查人
  reviewer_role VARCHAR(20),    -- 评查人角色
  review_date DATE,             -- 评查日期
  total_score DECIMAL(5,2),     -- 总得分
  grade VARCHAR(10),            -- 等级（优秀/合格/不合格）
  remark TEXT,                  -- 总体评语
  status ENUM('草稿','已提交') DEFAULT '草稿',
  create_time DATETIME,
  FOREIGN KEY (case_id) REFERENCES case_file(id)
);

-- 评分明细表
CREATE TABLE review_score_detail (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  review_id BIGINT,             -- 关联评查记录
  standard_id BIGINT,           -- 关联评查标准
  score DECIMAL(5,2),           -- 得分
  deduction_reason TEXT,        -- 扣分原因
  evidence_desc TEXT,           -- 证据描述
  FOREIGN KEY (review_id) REFERENCES review_record(id),
  FOREIGN KEY (standard_id) REFERENCES review_standard(id)
);

-- 用户表
CREATE TABLE user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) UNIQUE,
  password VARCHAR(200),        -- bcrypt加密
  real_name VARCHAR(50),
  org_name VARCHAR(100),       -- 所属单位
  role ENUM('admin','supervisor','reviewer','viewer'),
  is_active BOOLEAN DEFAULT TRUE,
  create_time DATETIME
);
```

---

## 三、功能模块设计

### 3.1 系统功能架构

```
生态环境案卷评查评分系统
├── 1. 用户权限管理
│   ├── 1.1 用户注册与登录
│   ├── 1.2 角色权限配置（管理员/评查员/查看者）
│   └── 1.3 组织机构管理
├── 2. 案卷管理
│   ├── 2.1 案卷录入
│   ├── 2.2 案卷批量导入（Excel模板）
│   ├── 2.3 案卷查询与筛选
│   └── 2.4 案卷附件管理（扫描件、图片）
├── 3. 评查标准配置
│   ├── 3.1 标准分类管理（程序/实体/文书）
│   ├── 3.2 检查项配置
│   ├── 3.3 评分规则设置
│   └── 3.4 标准模板（可按案卷类型选择）
├── 4. 评分功能
│   ├── 4.1 在线评查打分
│   ├── 4.2 逐项检查与扣分说明
│   ├── 4.3 评查意见填写
│   ├── 4.4 自动计算总分与等级
│   └── 4.5 评查进度跟踪
├── 5. 统计分析
│   ├── 5.1 评查完成情况概览
│   ├── 5.2 分数分布统计
│   ├── 5.3 问题类型分析
│   ├── 5.4 单位/部门横向对比
│   └── 5.5 趋势分析（同比/环比）
├── 6. 报告管理
│   ├── 6.1 评查报告生成
│   ├── 6.2 报告导出（Word/PDF）
│   └── 6.3 报告打印
└── 7. 系统管理
    ├── 7.1 案卷类型配置
    ├── 7.2 等级标准配置（优秀≥90/合格≥70/不合格<70）
    └── 7.3 数据备份与导出
```

### 3.2 评查标准示例（依据生态环境部标准）

| 分类 | 检查项 | 满分 | 评分规则 |
|------|--------|------|----------|
| **程序合规(40分)** | 立案审批 | 5分 | 缺少立案审批文书扣5分 |
| | 调查取证 | 10分 | 现场笔录不完整扣3分/项 |
| | 告知与听证 | 10分 | 未告知当事人权利扣10分 |
| | 集体讨论 | 5分 | 重大案件未集体讨论扣5分 |
| | 决定审批 | 10分 | 审批程序不规范扣5分/项 |
| **实体合法(40分)** | 事实认定 | 15分 | 事实不清扣5-15分 |
| | 法律适用 | 15分 | 适用法律错误扣15分 |
| | 裁量基准 | 10分 | 裁量明显不当扣5-10分 |
| **文书规范(20分)** | 文书格式 | 10分 | 格式不规范扣2分/项 |
| | 证据装订 | 5分 | 顺序错误扣1分/项 |
| | 归档完整 | 5分 | 缺少必要文书扣5分 |

---

## 四、界面原型设计

### 4.1 Web管理后台 - 首页仪表盘

```
┌─────────────────────────────────────────────────────────────────┐
│  🌿 生态环境案卷评查评分系统    [管理员 ▼] [退出登录]            │
├────────────┬────────────────────────────────────────────────────┤
│            │                                                    │
│ 📊 数据概览  │   今日评查进度                    单位得分排名     │
│ ───────────│   ┌─────────────────┐  ┌─────────────────┐        │
│ 📁 案卷管理  │   │ 待评查: 45件    │  │ 第1名: XX局 92分│        │
│ 📋 评查标准  │   │ 本月完成: 128件 │  │ 第2名: XX局 88分│        │
│ ✏️ 在线评分  │   │ 合格率: 94.5%   │  │ 第3名: XX局 85分│        │
│ 📈 统计分析  │   └─────────────────┘  └─────────────────┘        │
│ 📄 报告管理  │                                                    │
│ ⚙️ 系统设置  │   评查分数分布              问题类型分析          │
│            │   [柱状图/饼图]              [雷达图/柱状图]        │
│            │                                                    │
└────────────┴────────────────────────────────────────────────────┘
```

### 4.2 移动端 - 现场评查

```
┌──────────────────────┐    ┌──────────────────────┐
│ ◀ 行政处罚案卷 #2024  │    │ 评查进度  12/30 项   │
│        001           │    │ ████████░░░░░ 40%    │
├──────────────────────┤    └──────────────────────┘
│ 🔍 快速定位: [输入项  │
│        目名称...]    │
├──────────────────────┤    ┌──────────────────────┤
│                      │    │ ■ 程序合规 (40分)    │
│ ▶ 1. 立案审批        │    │   1.1 立案呈批表 ✓   │
│   ▶ 2. 调查取证      │    │   1.2 立案审批 ✓     │
│     2.1 现场笔录  [85分]│   │   1.3 案件来源说明 ✓│
│     2.2 调查询问  [90分]│   └──────────────────────┤
│     2.3 采样记录  [未评]│                            │
│   ▶ 3. 告知与听证      │   [保存草稿]  [提交评查]  │
│   ▶ 4. 集体讨论        │                            │
│   ▶ 5. 决定审批        │   评查历史 | 问题汇总 |   │
│ ▶ 6. 事实认定        │   报告预览                 │
│ ▶ 7. 法律适用        │                            │
│ ▶ 8. 文书规范        │                            │
└──────────────────────┘                            │
```

---

## 五、开发计划（分阶段）

### 第一阶段：基础框架搭建（2周）
- [ ] 项目初始化：前后端项目创建、Git仓库
- [ ] 数据库设计与创建
- [ ] 用户认证模块（登录、注册、JWT）
- [ ] 基础框架：路由、权限、中间件

### 第二阶段：核心功能开发（4周）
- [ ] 案卷管理模块（CRUD、导入导出）
- [ ] 评查标准配置模块
- [ ] 评分功能模块（核心）
- [ ] 评查记录管理

### 第三阶段：高级功能开发（3周）
- [ ] 统计分析模块（图表、数据看板）
- [ ] 报告生成与导出（Word/PDF）
- [ ] 移动端适配

### 第四阶段：测试与优化（2周）
- [ ] 功能测试与Bug修复
- [ ] 性能优化
- [ ] 安全加固
- [ ] 部署文档编写

### 第五阶段：交付部署（1周）
- [ ] 正式环境部署
- [ ] 数据迁移（如有）
- [ ] 用户培训
- [ ] 交付文档

---

## 六、项目估算

| 项目 | 估算 |
|------|------|
| **总工期** | 约12周（3个月） |
| **人力配置** | 前端1人 + 后端1人 + 测试0.5人 |
| **服务器** | 2核4G × 2台（Nginx+Node） |
| **预估工作量** | 约240人天 |

---

## 七、下一步建议

1. **需求确认**：请确认上述方案是否符合您的预期
2. **评查标准文件**：请提供生态环境部的评查标准文件（如果有）
3. **现有系统对接**：是否需要与现有案卷系统对接？
4. **部署环境确认**：部署在政务云还是自有服务器？

---

*方案版本：v1.0 | 制定日期：2026-05-13*
